package com.omnia.backend;

import org.junit.jupiter.api.Test;

/**
 * Test "marcador" del proyecto. Las pruebas reales viven junto a cada
 * componente: tests unitarios para los servicios (Mockito) y tests de
 * integración para los controllers ({@code @WebMvcTest}).
 */
class BackendApplicationTests {

    @Test
    void smoke() {
        // Intencionadamente vacío: la cobertura real está en los tests por componente.
    }
}
